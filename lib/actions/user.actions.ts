"use server";

export const signIn = async () => {
  try {
    // Mutation / Databese / Make fetch
  } catch (error) {
    console.log("Error", error);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    // Mutation / Databese / Make fetch
  } catch (error) {
    console.log("Error", error);
  }
};
